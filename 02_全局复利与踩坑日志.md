# 研压树洞｜全局复利与踩坑日志

## 使用规则

这里只记录可复用经验、已验证结论和真实踩坑。临时进度写入 03_STATUE.md，完整计划写入 01_项目步骤记录.md。未经验证的判断标注“待验证”。

## 可复用规则

1. 分别记录 requested_support_mode、recommended_route、effective_route。
2. 风险检测早于普通生成；输出校验只是第二道防线。
3. State Analyzer 先影子运行，通过 Evaluation Set 后再接入 Router。
4. Invalid JSON、缺失字段、枚举越界和超时必须显式失败；风险不得默认 low。
5. 日志最小化收集，不默认保存完整原文或非必要身份信息。
6. 本地与线上使用同一份 Prompt 逻辑，变更时同步版本和测试。
7. A/B Test 一次只改变一个核心变量，不降低安全保护强度。
8. 上下文裁剪只能发生在完整历史安全检测之后；摘要只保留稳定标签，不复制较早原文。
9. 合成 Evaluation Set 的高分只证明规则一致性，不能替代真实 Bad Case 泛化验证；新 Analyzer 必须先保持影子运行。
10. Policy Router 必须分别输出推荐路线和实际路线；Hard Safety 的实际路线不受用户模式、影子开关或普通 Router 覆盖。
11. Tool 结果只有通过输入和输出 Schema 校验后才能进入 Prompt；失败必须显式转为澄清，不能让模型补写一个看似成功的工具结果。
12. 短期 Memory 必须是有界、可过期、可清除的结构化标签；不得保存原文或医疗画像，也不得参与或覆盖当前请求的安全判定。
13. Response Validator 必须在首个模型字节返回用户前看到完整回复；否则跨分片的危险表达可能绕过校验。校验器自身失败时必须关闭失败，不能透传原文。
14. 实验分组必须 session 内稳定、一次只改变一个体验变量，并在分配器与执行入口两层排除 SAFETY；根因字段只能标为“假设”，不能把启发式归因当成事实。

## 已发现踩坑

### 2026-09-15｜高风险输入可能未在生成前切换安全路线

- 现象：前端能检测风险词，但普通后端请求仍可能先执行。
- 原因：风险检测与 mode 切换未在生成前统一阻断。
- 影响：高风险用户可能收到普通陪伴式回复。
- 解决方向：风险检测前置，高风险直接进入 Hard Safety Routing。
- 状态：已修复，Stage 0/1 回归测试通过。

### 2026-09-15｜计划与代码实现粒度不一致

- 现象：计划描述的风险分级、上下文、重试和反馈体系，比当前代码实现更完整。
- 原因：产品概念与可验证工程模块未统一定义。
- 复用规则：以代码、测试和数据字段共同作为“已完成”依据。
- 状态：规则已确认。

### 2026-09-15｜Prompt 存在重复实现风险

- 现象：独立 Prompt 文件与服务代码中存在相似构建逻辑。
- 影响：本地和线上策略可能漂移。
- 解决方向：阶段 0 统一来源。
- 状态：已处理，本地与线上统一使用 api/chat-handler.js 和 api/prompt.js。

### 2026-09-15｜本地反馈不等于完整数据闭环

- 现象：localStorage 无法跨设备汇总，也不能关联完整 Agent 决策链。
- 解决方向：保留本地 Demo，后续建立合规的结构化事件层。
- 状态：Stage 1 已定义结构化事件和本地反馈边界；当前不建立服务端对话库。

## 日志模板

### YYYY-MM-DD｜标题

- 场景：
- 现象：
- 原因：
- 解决方式：
- 复用规则：
- 验证方式：
- 验证结果：待验证／通过／失败
- 关联文件或 Case：
