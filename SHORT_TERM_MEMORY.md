# 研压树洞｜Stage 6 短期 Memory

## 目标

在同一临时会话内保持必要的对话连续性，同时避免建立长期画像或保存敏感原文。

## 保存范围

每个 session_id 只保存：

- 当前话题：科研、导师或未来
- 未完成事项：预定义任务标签
- 明确支持偏好：用户主动表达的预定义偏好
- 上次执行路线：SAFETY、CLARIFY、ACT 或 LISTEN

不保存对话原文、姓名学号等身份信息、情绪标签、风险标签、医疗判断或模型回复正文。所有写入均经过固定枚举过滤和 Schema 校验。

## 生命周期

- 存储位置：服务进程内存，不落盘、不跨设备
- 会话隔离：使用前端生成的临时 session_id
- 过期时间：最后更新后 30 分钟
- 容量上限：1000 个会话，超出时淘汰最早记录
- 主动删除：用户点击“清空对话”时调用 DELETE /api/memory/session
- 被动删除：进程重启后全部清空

## 执行顺序与安全边界

1. 当前请求仍先执行完整历史风险检测、State Analyzer 和 Policy Router。
2. 路线确定后才读取和更新 Memory。
3. SAFETY 路线使用固定安全回复，不把 Memory 发送给普通模型。
4. 非 SAFETY 路线仅注入通过校验的结构化标签，并明确禁止覆盖当前输入、安全策略和用户新选择。
5. Memory 读取、写入或格式化失败时只记录无正文的失败事件，不改变当前支持路线。

## 可观测性

事件只记录是否命中、各类标签数量、最后路线和错误类别，不记录记忆正文。模块版本为 short-term-memory-v1。

## 验收

- 同一会话的下一次请求可获得结构化连续性
- 私密原文不会进入 Memory 或 Memory Prompt
- 新请求声明任务完成后会移除旧的未完成事项
- 过期、容量淘汰和显式清除可验证
- 存储故障不影响普通支持和安全回复
- Stage 0–6 共 39 项测试通过
