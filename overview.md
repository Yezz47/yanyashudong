# 研压树洞｜产品优化分析报告

> 评审视角：资深产品经理　｜　日期：2026-07-31　｜　基于 PRD_Notion.md / PROMPT_Optimization.md / 前后端实现

---

## 一、项目概览

**研压树洞** 是面向研究生的 AI 情绪疏解 Web App，核心是完成「场景识别 → 情绪命名 → 温和解释 → 低负担行动」的闭环，把混乱的情绪表达整理成用户能看懂、能行动的结构。产品有明确边界：提供情绪支持，不替代心理咨询/精神科诊疗/紧急救助。同时承载作品集目标，展示 AI 产品经理的场景拆解与 Prompt 评测能力。

技术栈：纯原生 HTML/CSS/JS 前端 + Node 后端（DeepSeek 流式），Vercel/Render 可部署，无账号、无云端数据库。

---

## 二、核心信息梳理

### 1. 定位与核心目标
- 定位：研究生专属的 AI 情绪疏解工具（非泛用聊天）
- 目标：情绪混乱 → 结构化整理 → 低负担行动的闭环
- 边界：情绪支持，不做诊断、不替代诊疗

### 2. 目标用户及特征
- 核心：受科研/导师/未来三类压力困扰的研究生
- 状态：情绪混乱 + 缺少闭环，习惯忍着或刷手机转移
- 痛点：难命名情绪、泛用 AI 太泛太说教、倾诉后无整理

### 3. 核心功能模块
五页线性闭环，"情绪聊天室"是中枢：
- 欢迎页（认知与边界）→ 需求选择页（四模式）→ 情绪聊天室（核心）→ 情绪整理卡片（结构化输出）→ 反馈页（评测收集）
- 聊天室向上自动识别"科研/导师/未来"场景，向下承载"倾听/理解/行动/安全支持"四种模式
- 安全模式为独立紧急分支：检测自伤/自杀关键词即强制切入

### 4. 关键业务流程
选入口 → 对话（咨询式陪伴，每5轮轻柔回望）→ 整理（生成结构化卡片）→ 反馈（服务Prompt优化）。典型场景：组会被否定后倾诉 → 被承接 → 情绪命名 → 生成卡片 → 一个10-15分钟的小行动。

---

## 三、优点分析

### 优点 1：场景聚焦精准，差异化清晰
不做泛用聊天，死磕"研究生"这一细分群体的三类压力。在 AI 产品同质化严重的当下这是稀缺价值——通用大模型给的安慰太泛太说教，而研压树洞有明确的场景识别机制（关键词匹配 + AI 上下文）。边界克制也是优点：明确"不替代心理咨询"，这种"知道自己不做什么"的判断力是好产品的标志。

### 优点 2：闭环设计完整，情绪到行动有回路
多数情绪类产品止步于"倾诉"，研压树洞完成了完整闭环。情绪整理卡片是关键的结构化输出环节，把混沌对话变成可看懂的结构（场景/情绪/触发/解释/行动）。反馈页反向服务 Prompt 优化，形成"使用→反馈→优化"迭代回路。行动建议刻意限制为"10-15分钟一个动作"，避免长清单说教——这是对用户心理负担的真实尊重。

### 优点 3：Prompt 工程扎实，有兜底韧性
已做过一轮系统优化（从"产品闭环驱动"转向"咨询式陪伴驱动"），反映团队真在用反馈迭代。技术上兜底链完整：DeepSeek 流式 → 超时重试 → 本地模板兜底，保证 demo 不卡死。安全机制有基础：风险关键词触发安全模式。Prompt 规则细致（每轮只问1个问题、5轮轻柔回望、禁用元叙事词），专业度高。

---

## 四、缺点诊断与优化方案

### 缺点 1：用户画像偏粗，缺乏分层
**问题**：PRD 只说"研究生群体"，但内部差异巨大——硕士 vs 博士、研一 vs 研三 vs 延期、理工 vs 人文、国内 vs 海外。一个研一的科研焦虑和一个延毕博士的绝望，需要的支持强度完全不同。当前场景识别只分科研/导师/未来，缺少"压力阶段"维度。

**优化方案（经 PM 讨论修订）**：

> 关键决策：洞察"年级隐含场景倾向（研一→论文焦虑，研三→未来焦虑）"是对的，但落地方式从"前置问卷"改为"识别增强"——既不增加负担，又让场景识别更准，还守住"被听见"的核心体验。

- **不 upfront 询问年级/年龄**：焦虑用户对"被审问"敏感，前置问卷会造成抵抗心理，破坏"被听见"的第一感受
- **年级信号内化到场景识别**：对话中自然出现的年级信息（如"我研三了还在投简历""组会被导师骂"）作为场景识别的修正权重——研一提科研词→科研场景加权，研三提未来词→未来场景加权。比 upfront 问更准、更轻
- **情绪强度靠现有入口隐含**：需求选择页的"我现在状态很糟"入口本身就是高强度信号，选它直接进安全模式，无需额外分层维度
- **三大场景聚焦不细分**：保持科研/导师/未来三类，不细分硕士/博士/理工/人文，守住 MVP 克制

### 缺点 2：反馈数据未持久化，评测闭环未闭合（已修复）
**当前状态**：反馈已在 localStorage 中保留最近 100 条，可按场景、推荐/执行路线、模块版本和实验组聚合；已支持只改变结尾问题形式的稳定 A/B 分流。自由文本和 Bad Case 明细不上传服务端，真实样本量与统计显著性仍待后续验证。

**优化方案**：
- 轻量持久化：即便不做账号，可用 localStorage 存反馈，或加极简后端接口（/api/feedback 落 JSON 文件/轻量 DB）让反馈可聚合
- Prompt 版本管理：每次改 Prompt 打版本号，反馈带版本标识，才能真正做版本对比与回归
- 强化安全检测：扩充隐晦表达（"撑不下去""没意思了""解脱""一了百了"），引入风险等级分层，高危直接强提示 + 阻断普通对话

### 缺点 3：缺少可量化指标体系，验收偏功能实现
**问题**：PRD 验收标准全是"能否完成五页流程""能否调用 AI"这类功能可用性，没有衡量"产品是否真帮到用户"的指标。无北极星指标，无驱动/健康指标分层。作为作品集，会让评审觉得"功能完整但不知效果如何"。

**优化方案——三层指标体系**：
| 层级 | 指标 | 说明 |
|------|------|------|
| 北极星 | 被理解感高分率 | 反馈页"我感觉被理解"选"高"的占比，最代表核心价值 |
| 驱动指标 | 完整闭环完成率 | 走到反馈页的用户占比 |
|  | 情绪改善度 | 对话前后情绪命名变化（负→正） |
|  | 行动建议采纳意愿 | 用户是否愿意尝试下一步 |
| 健康指标 | 安全模式触发率 | 过高说明入口/承接有问题 |
|  | 兜底触发率 | 反映 AI 稳定性 |
|  | 平均对话轮次 | 过短可能没接住，过长可能低效 |

每个指标设基线与目标值，demo 阶段也能展示"用数据衡量产品效果"的 PM 思维。

---

## 五、优化优先级建议

| 优化项 | 影响 | 成本 | 优先级 |
|--------|------|------|--------|
| 强化安全检测与风险分层 | 高 | 低 | P0 |
| 指标体系设计与埋点 | 高 | 中 | P0 |
| 反馈数据持久化 | 高 | 中 | P1 |
| 年级信号内化场景识别 | 中 | 低 | P1 |
| Prompt 版本管理 | 中 | 低 | P2 |
| 留存/复访机制 | 低（MVP可缓） | 高 | P3 |

---

## 六、总评

研压树洞是一个**定位清晰、闭环完整、Prompt 专业度高**的作品集级 MVP。它的核心价值在于"懂研究生场景"的差异化，和"情绪→行动"的闭环设计。

主要短板集中在**数据闭环未真正闭合**（反馈不持久化、无指标体系）和**用户分层不足**。这些恰好是用较低成本就能显著提升"产品感"的环节——补上后，作品集叙事会从"我做了一个能跑的 demo"升级为"我用数据验证了产品效果并持续迭代"，这才是资深评审想看到的。

---

## 七、产品决策记录

### 决策 1：用户分层方式（2026-07-31 讨论确定）
- **背景**：原方案建议 upfront 问"压力阶段"做分层
- **讨论**：产品负责人提出可在入口问年级（研一→论文焦虑，研三→未来焦虑）做预判
- **最终决策**：不 upfront 问任何年龄/年级信息。年级洞察内化为"对话中年级信号→场景识别修正权重"
- **理由**：
  1. 焦虑用户对"被审问"敏感，前置问卷造成抵抗心理，破坏"被听见"核心体验
  2. 年级≠场景 1:1（研一也可能焦虑就业，研三也可能卡论文返修），upfront 问会误判
  3. 年级信号在对话中自然出现，内化到识别逻辑更准、更轻
  4. 情绪强度已由"我现在状态很糟"入口隐含，无需额外分层维度
- **共识方**：产品负责人 + PM

### 待推进决策
- 缺点 2（反馈持久化）与缺点 3（指标体系）方案已对齐，待选实施顺序
- 建议 P0 先行：安全检测强化 + 指标体系埋点
