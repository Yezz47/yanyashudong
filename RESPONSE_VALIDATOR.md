# 研压树洞｜Stage 7 Response Validator 与可靠性

## 目标

普通模型输出在到达用户前经过独立、可测试的服务端校验。Prompt 是第一道约束，Response Validator 是生成后的第二道边界。

## 校验结果

输出状态只有三种：

- pass：原样返回
- revise：自动收敛说教表达、连续追问或过长回复
- block：不返回模型原文，改用固定、非诊断性的支持回复

所有结果均通过封闭 Schema 校验。校验器抛错或返回非法结构时按 block 等级安全降级。

## 阻断规则

- 医疗或心理疾病诊断
- 疗效、治愈或必然好转承诺
- 带具体号码的虚构热线或联系方式
- 停药、停止治疗、自伤方法等危险建议
- 索取真实姓名、学号、身份证、详细住址或手机号

## 自动修订规则

- “你应该”“你必须”“别想太多”等说教表达改为保留选择权的说法
- 每轮最多保留两个问题
- 安全上限为 600 字；Prompt 仍要求普通轮次优先控制在 90–160 字

## 可靠性边界

- 上游超时或网络失败：502，并标记 timeout 或 network_error
- HTTP 限流或服务错误：502，保留上游状态分类
- 空响应：502 empty_response
- 流读取中断：502 stream_error，不释放已缓存的部分原文
- 非法 SSE JSON：502 invalid_upstream_json
- Task Planning Tool 失败：转 CLARIFY，不伪造工具结果
- Memory 失败：忽略记忆，继续当前安全或支持路线
- Validator 失败：固定安全替代回复，不透传模型原文

为保证跨分片表达也能被识别，服务端先缓冲完整上游回复，再向原有流式 HTTP 接口写出校验后的文本。这会增加首字延迟，但避免危险片段先于校验到达用户。

## 可观测性与隐私

response_validated 只记录状态、预定义违规代码及输入输出长度；response_validator_failed 只记录错误类别。两者均不记录模型回复正文。

模块版本：response-validator-v1。

## 验收结果

- 跨两个流分片的诊断表达可在原文到达用户前阻断
- 不合法的 Validator 输出不会被透传
- 超时、限流、空响应、流中断和非法 JSON 均有测试
- Stage 0–7 共 47 项测试通过
