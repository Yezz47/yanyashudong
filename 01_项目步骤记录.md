# 研压树洞｜项目步骤记录

## 总体目标

将当前“用户输入 → LLM 直接回复”的产品，升级为具备上下文构建、状态识别、策略路由、工具调用、短期记忆、安全兜底、可观测性和持续评测能力的研究生 AI 情绪支持 Agent。

边界：只提供情绪支持和低负担行动协助；不诊断、不承诺疗效，不替代心理咨询、精神科诊疗或紧急救助。

## 当前基线

已具备：Hard Safety、结构化可观测性、Context Builder、影子 State Analyzer、Policy Router、Task Planning Tool、短期 Memory、Response Validator、Bad Case 体系、50 条合成 Evaluation Set、单变量实验分流和模块版本管理。

生产化待验证：真实脱敏样本上的分类与路由表现、足量实验样本的统计显著性、部署平台日志权限与保留期，以及正式数据存储的访问控制和删除机制。

## 实施原则

1. 安全优先于体验、行动、数据和实验。
2. 每阶段只解决一个明确问题，并有独立验收标准。
3. 用户选择、系统判断和最终执行路线分开记录。
4. 结构化模型输出必须经过 Schema 校验。
5. 未知、解析失败或模型失败不得静默降级为低风险。
6. 原始对话与结构化埋点分开，默认最小化收集敏感信息。
7. 保留当前流式体验和本地降级能力，渐进式升级。
8. 新分类器先影子运行，通过评测后再影响正式回复。

## 分阶段计划

### 阶段 0：安全闭环与版本基线

前置风险检测；高风险强制进入 SAFETY；禁止普通 Router、Tool 和普通生成；统一 Prompt 来源；增加 app、prompt、analyzer、router 版本字段；补充安全、正常输入、空响应和降级测试。

验收：高风险输入不会进入普通回复链路，安全链路异常时使用固定安全兜底。

### 阶段 1：数据层与可观测性

定义会话、消息和事件结构；记录延迟、重试、降级、安全触发和最终路线；建立反馈与 Bad Case 结构；明确脱敏、保存范围和保留周期。

验收：可按 session_id 还原模块执行结果，不采集非必要身份信息。

### 阶段 2：Context Builder

限制上下文轮数；提炼话题、未完成任务和明确偏好；长对话使用摘要；支持清空状态。

### 阶段 3：State Analyzer 影子运行

定义 scene、intent、emotion、need、risk 的枚举和 JSON Schema；输出置信度与简短依据；建立 50–100 条 Evaluation Set。

验收：可计算分类准确率、Risk Recall 和 Risk False Positive Rate。

### 阶段 4：Policy Router

优先级：high risk → SAFETY；信息不足 → CLARIFY；明确行动需求 → ACT；倾诉需求 → LISTEN；无法判断 → CLARIFY。

### 阶段 5：Task Planning Tool

高风险禁止调用；信息不足先澄清；输入输出均校验；默认一个低负担下一步；失败时降级且不伪造结果。

### 阶段 6：短期 Memory

仅保存当前话题、未完成任务、明确支持偏好和上次执行路线，不建立医疗标签或敏感长期画像。

### 阶段 7：Response Validator 与可靠性

检查诊断、疗效承诺、虚构求助信息、危险建议、回复过长、连续追问和说教表达；处理超时、限流、空响应、流中断、Invalid JSON 和 Tool Failure。

### 阶段 8：Bad Case、分析与实验

建立分类及根因模板；按场景、路线和版本分析；开展单变量 A/B Test；安全策略不参与弱化型实验。

## 核心指标

- 北极星：被理解感高分率
- 体验：继续对话率、平均轮数、主动结束率、负反馈率
- Agent：分类准确率、路由准确率、工具成功率
- Safety：Risk Recall、Risk False Positive Rate、Unsafe Response Rate
- Reliability：API Success Rate、Retry Rate、Fallback Rate、P95 Latency

## 变更记录

| 日期 | 阶段 | 变更 | 验证结果 |
|---|---|---|---|
| 2026-09-15 | 规划 | 根据当前代码基线建立实施路线 | 已完成 |
| 2026-09-15 | 阶段 0 | 前置风险检测与 Hard Safety Routing；统一 Prompt/聊天入口；增加模块版本；补回归测试 | 6 项测试通过 |
| 2026-09-15 | 阶段 1 | 定义会话、消息、事件、反馈和 Bad Case；记录路由、重试、降级与耗时；明确隐私和保留边界 | 9 项测试通过 |
| 2026-09-15 | 阶段 2 | 限制普通模型上下文；提炼较早主题、未完成事项和支持偏好；支持清空会话；保持全历史安全检测 | 13 项测试通过 |
| 2026-09-15 | 阶段 3 | 定义 State Schema；输出置信度和依据；影子运行；建立 50 条合成 Evaluation Set | 16 项测试通过；Scene 96%，Risk Recall 100%，Risk FPR 0% |
| 2026-09-15 | 阶段 4 | 实现四级 Policy Router、路线字段分离、影子开关及故障安全策略 | 23 项测试通过；50 条合成集路由一致性 100% |
| 2026-09-16 | 阶段 5 | 实现 Task Planning Tool；输入输出校验；高风险禁用；信息不足澄清；单个低负担行动；失败不伪造 | 32 项测试通过 |
| 2026-09-16 | 阶段 6 | 实现进程内短期 Memory；仅保存四类结构化字段；支持 30 分钟过期、容量淘汰、任务完成移除和显式清除 | 39 项测试通过 |
| 2026-09-16 | 阶段 7 | 实现 Response Validator；阻断诊断、疗效承诺、虚构求助信息、危险建议和隐私索取；修订过长、连续追问和说教表达；补齐异常降级 | 47 项测试通过 |
| 2026-09-16 | 阶段 8 | 建立 Bad Case 分类与根因假设；按场景、推荐/执行路线、版本和实验组本地聚合；上线单变量稳定分流并双重排除 SAFETY | 55 项测试通过 |
